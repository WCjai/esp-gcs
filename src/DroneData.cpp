#include "DroneData.h"

// Initialize the drone data with default values.
DroneData droneData = {0, 0, 0, 0, 0, 0, false, false, "UNKNOWN", 0.0};

// Initialize the status text array.
String lastStatusText[3] = {"", "", ""};
